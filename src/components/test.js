import React from 'react';
import { Link } from 'react-router-dom';

const Test = (props) => (
    <div>
        <Link to={`/`}>
            Volver a Login
        </Link>
    </div>
)

export default Test;